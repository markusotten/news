# Skill defects

Defects in the `citrix-product-update` skill and the sources it depends on. This file is the only place they are recorded — the daily and weekly notes never carry a Skill-Bug section.

Each entry has a stable id. Runs update `Last seen` for defects that still occur and move cleared ones to Resolved.

## Open

## Resolved

### phase4-cache-hash-scheme-unreproducible — this run's What's New extraction cannot be reconciled against `.whatsnew-cache.json`
- 2026-08-16 → 2026-08-17 · reseeded `citrix/.whatsnew-cache.json` with a new, documented extraction method rather than chasing the unrecoverable 2026-08-14/2026-08-15 script (it was never committed, only its cache output was, so option (a) — locate and reuse it — was not viable).
- **Method (now the standing method for this skill):** fetch each fetchable product's What's New URL with `curl`. Parse with Python's stdlib `html.parser` into a lightweight tree, strip `script`/`style`/`nav`/`header`/`footer`/`aside`, then isolate the article body as the `<div class="sub-content-main">` block — present on every `docs.citrix.com`/`docs.xenserver.com` content page checked (confirmed on citrix-daas, citrix-cloud, storefront, workspace-app, secure-mail, enterprise-browser, xenserver/9); `docs.netscaler.com` landing pages have no such wrapper (they use styled `div`s instead of headings for their nav cards) so the whole `<body>` is used there instead, minus the same stripped tags. Within that region: if the page contains 3+ `<p>` elements whose first child is `<strong>` (excluding any nested inside `<li>`/`<td>`, and excluding inline callout labels — Note/Important/Benefits/Warning/Tip/Caution/Prerequisites/Requirements/Limitations/Example(s)) — the "monthly narrative" style used by citrix-daas, citrix-cloud, citrix-secure-mail, etc. — each such paragraph is one entry (title = the `<strong>` text, first body line = the rest of that paragraph). Otherwise, fall back to heading-based entries at the finest heading level (h4, else h3, else h2) that occurs more than once — the "one heading per version/topic" style used by citrix-workspace-app, storefront, citrix-enterprise-browser, xenserver — excluding a small denylist of purely structural section labels (New and enhanced features / Fixed issues / Known issues / Removed features / Deprecated features / Requirement / Removed). Hash = first 12 hex chars of `md5(lowercased "title|first body line")`. Cache keeps the 200 most recent hashes per product (page order is newest-first on every page checked).
- Ran this against all 47 fetchable products (46 from the prior run's catalog, plus the newly-added XenServer SDK — which is `none`, so not fetched). All 47 returned HTTP 200 and produced plausible per-entry titles (spot-checked citrix-daas, citrix-workspace-app, xenserver-9). Citrix DaaS's raw count is genuinely large (838 before the 200-cap) because its page runs monthly release notes back to 2016; a handful of nested-list sub-bullets (e.g. a "Protocol:" / "Frames Per Second:" pair inside one entry's own body table) still slip through as extra low-signal entries — a known, accepted imprecision, not a crash or a mass-miscount like the prior run's 1606/574.
- Per the skill's seeding rule, **reported nothing as new** from this reseed pass for any of the 47 products. Independently — not relying on the hash scheme at all — scanned every fetched page's plain text for exact day-level dates (`YYYY-MM-DD` and `Month D, YYYY`) inside the 2026-08-16T02:30Z–2026-08-17T02:11Z window; none were found on any of the 47 pages, so this reseed did not silently drop a same-day dated entry.
- **Consequence for readers:** Phase 4 coverage for 2026-08-16 (the prior run, which also reported nothing pending this fix) and this reseed pass on 2026-08-17 should both be read as "no diff available" for What's New content, not "confirmed nothing changed." Diffing resumes cleanly from the 2026-08-18 run onward, now against a same-method baseline.

### catalog-missing-whatsnew-column — `citrix-products.md` lacked the dedicated What's New URL column the skill requires
- 2026-08-13 → 2026-08-15 · restructured `citrix-products.md` to the 3-column `Product | Docs URL | What's New URL` form. Re-verified every URL from the 2026-08-13/2026-08-14 research (HTTP 200 + body contains "What's new", allowing for the curly-apostrophe "What's new" some docs.citrix.com pages use) and resolved the ~22 products the prior research hadn't covered (`Citrix Aidrien`, `Citrix Cloud Government`, `Client app management`, `Citrix DaaS Standard for Azure`, `Global App Configuration Service`, `Citrix HDX Plus for Windows 365`, `ITSM Adapter for ServiceNow`, `Citrix Receiver`, `Citrix Remote Browser Isolation`, `Citrix Secure Hub`, `Citrix Secure Mail`, `Citrix Secure Web`, `Session Remote Start`, `StoreFront Cloud`, `Citrix Virtual Apps and Desktops`, `Citrix Virtual Delivery Agent for macOS`, `Citrix Workspace app`, `Citrix Workspace app for visionOS`, `XenApp and XenDesktop (legacy)`, `XenMobile (legacy)`, `XenServer 8.4`, `XenServer 9`). New findings: `Citrix Receiver` and `XenApp and XenDesktop (legacy)` genuinely have no what's-new page → `none` (Receiver's own nav points to Workspace app's page instead). `Session Remote Start` has no reachable what's-new content but isn't ruled out → `unverified:`. `Citrix Secure Hub` resolved to `overview.html#whats-new-in-the-current-version`. `Global App Configuration Service` shares StoreFront Cloud's `citrix-workspace/whats-new.html` (its own landing page's "What's new" link points there). `Citrix App Layering`, `Session Recording Service`, `Citrix Secure Private Access` and `Unicon eLux Scout` confirmed as `unverified:` per the prior run's finding (numbered doc-set page, service-overview redirect, legacy-archive page, and a versioned `2607/` folder respectively — the last does contain valid content today but is unstable across releases). All other resolutions from the 2026-08-13/2026-08-14 research reconfirmed unchanged.

### whatsnew-cache-never-created — `.whatsnew-cache.json` does not exist despite skill requiring it
- 2026-08-14 → 2026-08-15 · seeded `citrix/.whatsnew-cache.json` in Phase 4 of this run: fetched every non-`none`/non-`unverified` product's what's-new page, hashed each entry (heading + first body line), and wrote the full baseline. Per the skill's seeding rule, no entries were reported as "new" from this pass except entries that were both dated and inside the run window.

### phase4-narrow-window-sampling — What's New sweep not exhaustive on short-window runs
- 2026-08-13 → 2026-08-14 · this run built a curl-based fetch script (with landing-page link discovery and multiple guessed-suffix fallbacks) and swept all 62 catalog products in one run instead of sampling ~14 per short window. No entries dated inside the 2026-08-13T14:23:36Z–2026-08-14T02:12:53Z window were found on any of them. Reopen if a future short-window run goes back to partial sampling.
