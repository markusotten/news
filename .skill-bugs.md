# Skill defects

Defects in the `citrix-product-update` skill and the sources it depends on. This file is the only place they are recorded — the daily and weekly notes never carry a Skill-Bug section.

Each entry has a stable id. Runs update `Last seen` for defects that still occur and move cleared ones to Resolved.

## Open

### github-org-repos-scoped — api.github.com org listing blocked by session proxy
- First seen: 2026-08-13
- Last seen: 2026-08-13
- Expected: `GET https://api.github.com/orgs/citrix/repos?sort=pushed` returns the org's repos so Phase 6 can spot new/updated Citrix GitHub repos.
- Actual: The session's GitHub proxy rejects the call — "sessions are bound to their configured repositories. Use repository-scoped endpoints" — because this session's GitHub access is scoped only to `markusotten/news`.
- Run did: skipped the GitHub-repos portion of Phase 6 community coverage; everything else in Phase 6 (Tech Zone articles/blogs, community events, YouTube) completed normally.

### phase4-narrow-window-sampling — What's New sweep not exhaustive on short-window runs
- First seen: 2026-08-13
- Last seen: 2026-08-13
- Expected: Phase 4 fetches the What's New / release-notes page for all 61 products in `citrix-produts.md` every run.
- Actual: This run's window was only ~1h47m (a same-day follow-up to an earlier run today), so Phase 4 checked a representative sample of ~14 high-traffic products (Citrix Virtual Apps and Desktops, Citrix DaaS, Citrix Workspace app for Windows, NetScaler Console service, NetScaler ADC, NetScaler Console, Client app management, LAS for NetScaler, Citrix Secure Private Access, Citrix Provisioning, Citrix Cloud, and others) rather than the full catalog — every one checked confirmed no entries dated inside the window. Full 61-product coverage should resume on the next run with a normal (multi-hour/24h) window.
- Run did: reported no What's New items for this window; no items were dropped since none were found, but coverage of the remaining ~47 products was not independently verified this run.

## Resolved

_Entries are kept for 30 days after resolution, then deleted._
